app: srs-version
stage: dev

component: apigateway
name: apigw-service

inputs:
  region: ${env:REGION}
  serviceName: apigw_service
  environment: release
  protocols:
    - http
    - https
  endpoints:
    - path: /helloworld
      protocol: HTTP
      method: ANY # GET POST PUT DELETE HEAD ANY
      apiName: 'mock-api'
      serviceType: MOCK
      serviceMockReturnMessage: '{"code": 0, "data": {"module": "HTTP/HTTPS API Gateway", "path": "/service/v1/releases"}}'

# https://github.com/serverless-components/tencent-apigateway/blob/master/docs/configure.md
